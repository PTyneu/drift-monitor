# ── Databases ──────────────────────────────────────────────────
databases:
  - label: "test"
    host: "localhost"
    port: 5432
    dbname: "defects_test"
    user: "reader"
    password: "reader"
    table: "public.defect_results"
    timestamp_column: "created_at"

  - label: "prod"
    host: "localhost"
    port: 5432
    dbname: "defects_prod"
    user: "reader"
    password: "reader"
    table: "public.defect_results"
    timestamp_column: "created_at"

# ── Storage ─────────────────────────────────────────────────────
storage:
  dir: "storage"
